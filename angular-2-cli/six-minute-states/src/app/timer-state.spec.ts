/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {TimerState} from './timer-state';

describe('TimerState', () => {
  it('should create an instance', () => {
    expect(new TimerState()).toBeTruthy();
  });
});
